version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: mongodb_dev
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_dev_data:/data/db

volumes:
  mongo_dev_data:
    name: mongo_dev_data
